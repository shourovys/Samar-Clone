import React from 'react';
import { PaddingContainer } from '../../style/common';

const Portfolio = () => {
    return (
        <PaddingContainer>
            hello
        </PaddingContainer>
    );
};

export default Portfolio;